context:
    - name: nrf5x
      parent: cortexm

      env:
          includes:
              - ${relpath}/include
          LINKFLAGS:
              - -Tcortexm.ld

module:
    - name: nrf5x
      context: nrf5x
      depends:
          - cortexm
          - periph_common

      sources:
          - clock.c

subdirs:
    - periph
