contexts:
    - name: nrf5x
      parent: cortexm

      env:
          includes:
              - ${relpath}/include
          LINKFLAGS:
              - -Tcortexm.ld

modules:
    - name: nrf5x
      context: nrf5x
      depends:
          - cortexm
          - periph_common

      sources:
          - clock.c

    - name: feature/riotboot
      context: nrf5x

subdirs:
    - periph
