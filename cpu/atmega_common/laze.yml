context:
  - name: atmega_common
    parent: avr8_common
    env:
      includes:
        - ${relpath}/include
      libs:
        - -lm

defaults:
  module:
    context: atmega_common

module:
  - name: atmega_common
    depends:
      - core
    sources:
      - atmega_cpu.c

  - name: feature/periph_cpuid
  - name: feature/periph_eeprom
  - name: feature/periph_pm

subdirs:
  - periph
