module:
  - name: avr_libc_extra
    sources:
      - avr8_stdio.c
      - posix_unistd.c
    depends:
      - board
      - stdio_uart
    env:
      global:
        CFLAGS:
          - -isystem${relpath}/include
          - -isystem${relpath}/include/vendor
          - -I${root}/sys/libc/include
