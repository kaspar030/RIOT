module:
    name: newlib
    sources: syscalls.c
    global_vars:
        CFLAGS:
            - -I${relpath}/include
        LINKFLAGS:
            - -specs=nano.specs
    depends:
        - stdio_uart
    uses:
        - board
        - lto
