module:
  - name: newlib
    sources:
      - syscalls.c
    env:
      global:
        CFLAGS:
            - -specs=nano.specs
    depends:
        - stdio_uart
    uses:
        - board
        - lto
        - vfs
        - xtimer
