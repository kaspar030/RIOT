module:
    - name: sock_async_event
      sources:
          - sock_async_event.c
      depends:
          - event
          - sock_async
      env:
        export:
          includes:
              - ${relpath}
