module:
    - name: gcoap
      sources:
        - gcoap.c
      depends:
        - nanocoap
        - gnrc_sock_udp
        - gnrc_sock_async
        - sock_async_event
        - sock_util
        - event_timeout_ztimer
        - event_callback
