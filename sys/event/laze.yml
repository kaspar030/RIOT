modules:
    - name: event
      sources:
        - event.c
      depends:
        - core_thread_flags
      uses:
        - xtimer

    - name: event_callback
      sources:
          - callback.c
      depends:
          - event

    - name: event_periodic
      sources:
          - periodic.c
      depends:
          - event

    - name: event_timeout_ztimer
      sources:
          - timeout_ztimer.c
      depends:
          - event
          - ztimer_usec
