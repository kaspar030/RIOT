defaults:
    app:
      context: riot
      sources: [ main.c ]

subdirs:
    - asymcute_mqttsn
    - default
    - hello-world
    - gnrc_minimal
    - gnrc_networking
    - ipc_pingpong
    - nimble_gatt
    - saul
