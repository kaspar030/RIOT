apps:
    - depends:
          - asymcute
          - gnrc_sock_udp
          - sock_util

          # this pulls in GNRC's default configuration
          - gnrc_default

          # Add also the shell, some shell commands
          - shell
          - shell_commands
          - ps
          #
          - develhelp

      blocklist:
          # insufficient memory
          - atmega_common
