module:
    - name: at
      depends:
          - fmt
          - isrpipe_read_timeout
          - periph_uart
      uses:
          - at_urc
      sources:
          - at.c

    - name: at_urc
      depends: at
