modules:
    - name: boards_common_atmega
      depends:
          - atmega_common
      uses:
          - avr_libc_extra
      env:
        export:
          includes:
            - ${relpath}/include
