module:
    - name: boards_common_atmega
      sources:
          - board.c
      depends:
          - atmega_common
      uses:
          - avr_libc_extra
      env:
        export:
          includes:
            - ${relpath}/include
