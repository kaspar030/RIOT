app:
    depends:
        - gnrc_ipv6
        - gnrc_sock_ip
        - ps
    sources:
      - stack.c
    global_vars:
        CFLAGS:
            - -DGNRC_PKTBUF_SIZE="200"
            - -DTEST_SUITES
    blacklist:
        # insufficient memory
        - atmega_common
