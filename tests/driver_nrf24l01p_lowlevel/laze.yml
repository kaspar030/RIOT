apps:
  - depends:
        - nrf24l01p
        - ps
        - shell
        - shell_commands
        - xtimer
    env:
      global:
        CFLAGS:
            - -DSPI_PORT="SPI_DEV(0)"
            - -DCE_PIN="GPIO_PIN(0,0)"
            - -DCS_PIN="GPIO_PIN(0,1)"
            - -DIRQ_PIN="GPIO_PIN(0,2)"
