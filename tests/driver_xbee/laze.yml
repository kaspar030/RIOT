apps:
  - depends:
        - auto_init_gnrc_netif
        - gnrc_netif
        - gnrc_pktdump
        - gnrc_txtsnd
        - shell
        - shell_commands
        - xbee
    env:
      global:
        CFLAGS:
            - -DGNRC_PKTBUF_SIZE="512"
            - -I${relpath}
