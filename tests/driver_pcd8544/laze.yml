app:
    depends:
        - pcd8544
        - shell
    export_vars:
        CFLAGS:
            - -DTEST_PCD8544_SPI="SPI_DEV(0)"
            - -DTEST_PCD8544_CS="GPIO_PIN(0,0)"
            - -DTEST_PCD8544_RESET="GPIO_PIN(0,1)"
            - -DTEST_PCD8544_MODE="GPIO_PIN(0,2)"
