app:
    depends:
        - embunit
        - test_utils_interactive_sync
        - gnrc_sixlowpan_frag
    uses:
        - gnrc_ipv6
    global_vars:
        CFLAGS:
            - -DTEST_SUITES
            - -DGNRC_PKTBUF_SIZE="2048"
    vars:
        CFLAGS:
            - -I${root}/sys/net/gnrc/network_layer/sixlowpan/frag/

    blacklist:
        # insufficient memory
        - atmega_common
