app:
    depends:
        - auto_init_gnrc_netif
        - gnrc_ipv6_default
        - gnrc_netdev_default
        - gnrc_tcp
    export_vars:
        CFLAGS:
            - -DTARGET_ADDR=\"fe80::affe\"
            - -DTARGET_PORT="80"
            - -DCYCLES="3"
