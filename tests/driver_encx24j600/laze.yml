app:
    depends:
        - auto_init_gnrc_netif
        - encx24j600
        - gnrc_icmpv6_echo
        - gnrc_ipv6_router_default
        - periph_gpio
        - periph_spi
        - ps
        - shell
        - shell_commands
    global_vars:
        CFLAGS:
            - -DGNRC_PKTBUF_SIZE="2048"
            - -DENCX24J600_SPI="SPI_DEV(0)"
            - -DENCX24J600_CS="GPIO_PIN(0,0)"
            - -DENCX24J600_INT="GPIO_PIN(0,1)"
            - -I${relpath}
