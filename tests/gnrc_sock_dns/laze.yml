app:
  - depends:
        - auto_init_gnrc_netif
        - gnrc_ipv6_default
        - gnrc_netdev_default
        - gnrc_sock_udp
        - posix_headers
        - posix_inet
        - shell
        - shell_commands
        - sock_dns

    blacklist:
        # insufficient memory
        - atmega_common
