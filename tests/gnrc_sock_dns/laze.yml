app:
  - depends:
        - gnrc_netdev_default
        - gnrc_ipv6_default
        - gnrc_sock_udp
        - posix_headers
        - shell
        - shell_commands
        - sock_dns

    blocklist:
        # insufficient memory
        - atmega_common
