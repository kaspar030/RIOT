app:
  - depends:
        - tests-luid

module:
    - name: tests-luid
      sources: [tests-luid.c]
      depends:
          - luid
          - unittests-base
      env:
        global:
          testsuites:
              - luid
