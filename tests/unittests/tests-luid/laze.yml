apps:
  - depends:
        - tests-luid

modules:
    - name: tests-luid
      sources: [tests-luid.c]
      depends:
          - luid
          - unittests-base
      env:
        global:
          testsuites:
              - luid
