app:
    - depends:
        - tests-uuid

module:
    - name: tests-uuid
      sources: [tests-uuid.c]
      depends:
          - uuid
          - unittests-base
      env:
        global:
          testsuites:
              - uuid
