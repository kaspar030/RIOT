apps:
    - depends:
        - tests-uuid

modules:
    - name: tests-uuid
      sources: [tests-uuid.c]
      depends:
          - uuid
          - unittests-base
      env:
        global:
          testsuites:
              - uuid
