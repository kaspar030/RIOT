apps:
  - depends:
        - tests-ipv4_addr

modules:
    - name: tests-ipv4_addr
      sources: [tests-ipv4_addr.c]
      depends:
          - ipv4_addr
          - unittests-base
      env:
        global:
          testsuites:
              - ipv4_addr
