app:
  - depends:
        - tests-gcoap
    blocklist:
        # insufficient memory
        - atmega_common

module:
    - name: tests-gcoap
      sources: [tests-gcoap.c]
      depends:
          - gcoap
          - gnrc_ipv6
          - random
          - unittests-base
      env:
        global:
          testsuites:
              - gcoap
