app:
  - depends:
        - tests-bloom
    blacklist:
        # insufficient memory
        - atmega_common
module:
    - name: tests-bloom
      sources:
        - tests-bloom.c
      depends:
          - bloom
          - unittests-base
      env:
        global:
          testsuites:
              - bloom
