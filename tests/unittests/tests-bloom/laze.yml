app:
    depends:
        - tests-bloom
module:
    - name: tests-bloom
      sources:
        - tests-bloom.c
      depends:
          - sys/bloom
          - unittests-base
      global_vars:
          testsuites:
              - bloom
