apps:
  - depends:
        - tests-fib_sr

modules:
    - name: tests-fib_sr
      sources:
          - tests-fib_sr.c
      depends:
          - fib
          - unittests-base
          - xtimer
      env:
        global:
          testsuites:
              - fib_sr
          CFLAGS:
              - -DFIB_DEVEL_HELPER -DUNIVERSAL_ADDRESS_SIZE=16 -DUNIVERSAL_ADDRESS_MAX_ENTRIES=40
