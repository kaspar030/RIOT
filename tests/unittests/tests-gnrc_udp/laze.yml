apps:
  - depends:
        - tests-gnrc_udp
    blocklist:
        # insufficient memory
        - atmega_common

modules:
    - name: tests-gnrc_udp
      sources: [tests-gnrc_udp.c]
      depends:
          - gnrc_udp
          - gnrc_ipv6
          - unittests-base
      env:
        global:
          testsuites:
              - gnrc_udp
