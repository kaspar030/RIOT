app:
  - depends:
        - tests-gnrc_udp
    blacklist:
        # insufficient memory
        - atmega_common

module:
    - name: tests-gnrc_udp
      sources: [tests-gnrc_udp.c]
      depends:
          - gnrc_udp
          - gnrc_ipv6
          - unittests-base
      env:
        global:
          testsuites:
              - gnrc_udp
