apps:
    - depends:
        - tests-pkt

modules:
    - name: tests-pkt
      sources: [tests-pkt.c]
      depends:
          - gnrc_ipv6
          - test_suites_define
          - unittests-base
      env:
        global:
          testsuites:
              - pkt
