app:
    depends: tests-bitfield
module:
    - name: tests-bitfield
      sources:
        - tests-bitfield.c
      depends:
          - sys/bitfield
          - unittests-base
      global_vars:
          testsuites:
              - bitfield
