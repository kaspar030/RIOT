app:
    - depends:
        - tests-bitfield
module:
    - name: tests-bitfield
      sources:
        - tests-bitfield.c
      depends:
          - sys/bitfield
          - unittests-base
      env:
        global:
          testsuites:
              - bitfield
