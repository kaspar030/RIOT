apps:
    - depends:
        - tests-tsrb

modules:
    - name: tests-tsrb
      sources: [tests-tsrb.c]
      depends:
          - tsrb
          - unittests-base
      env:
        global:
          testsuites:
              - tsrb
