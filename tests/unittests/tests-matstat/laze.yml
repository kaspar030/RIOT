apps:
  - depends:
        - tests-matstat

modules:
    - name: tests-matstat
      sources: [tests-matstat.c]
      depends:
          - matstat
          - unittests-base
      env:
        global:
          testsuites:
              - matstat
