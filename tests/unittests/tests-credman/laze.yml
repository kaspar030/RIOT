apps:
  - depends:
        - tests-credman

modules:
    - name: tests-credman
      sources:
          - tests-credman.c
      depends:
          - credman
          - credman_load
          - test_suites_define
          - unittests-base
      env:
        global:
          testsuites:
            - credman
