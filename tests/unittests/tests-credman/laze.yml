app:
  - depends:
        - tests-credman

module:
    - name: tests-credman
      sources:
          - tests-credman.c
      depends:
          - credman
          - test_suites_define
          - unittests-base
      env:
        global:
          testsuites:
            - credman
