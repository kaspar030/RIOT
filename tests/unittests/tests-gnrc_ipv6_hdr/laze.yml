app:
    depends:
        - tests-gnrc_ipv6_hdr

module:
    - name: tests-gnrc_ipv6_hdr
      sources: tests-gnrc_ipv6_hdr.c
      depends:
          - gnrc_ipv6_hdr
          - unittests-base
          - test_suites_define
      global_vars:
          testsuites:
              - gnrc_ipv6_hdr
