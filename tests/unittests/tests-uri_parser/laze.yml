apps:
    - depends:
        - tests-uri_parser

      blocklist:
      # insufficient memory
        - atmega_common

modules:
    - name: tests-uri_parser
      sources:
          - tests-uri_parser.c
      depends:
          - uri_parser
          - unittests-base
      env:
        global:
          testsuites:
              - uri_parser
