apps:
  - depends:
        - tests-ipv6_hdr

modules:
    - name: tests-ipv6_hdr
      sources: [tests-ipv6_hdr.c]
      depends:
          - ipv6_hdr
          - inet_csum
          - unittests-base
      env:
        global:
          testsuites:
              - ipv6_hdr
