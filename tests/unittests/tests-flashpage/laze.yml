app:
  - depends:
      - tests-flashpage

module:
    - name: tests-flashpage
      sources:
          - tests-flashpage.c
      depends:
          - unittests-base
      env:
        global:
          testsuites:
              - flashpage
