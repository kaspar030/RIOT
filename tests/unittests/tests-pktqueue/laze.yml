app:
    - depends:
        - tests-pktqueue

module:
    - name: tests-pktqueue
      sources: [tests-pktqueue.c]
      depends:
          - pktqueue
          - unittests-base
      env:
        global:
          testsuites:
              - pktqueue
