apps:
    - depends:
        - tests-pktqueue

modules:
    - name: tests-pktqueue
      sources: [tests-pktqueue.c]
      depends:
          - pktqueue
          - unittests-base
      env:
        global:
          testsuites:
              - pktqueue
