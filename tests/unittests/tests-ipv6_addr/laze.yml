apps:
  - depends:
        - tests-ipv6_addr

modules:
    - name: tests-ipv6_addr
      sources: [tests-ipv6_addr.c]
      depends:
          - ipv6_addr
          - unittests-base
      uses:
          - ipv4_addr
      env:
        global:
          testsuites:
              - ipv6_addr
