apps:
    - depends:
        - tests-kernel_defines

modules:
    - name: tests-kernel_defines
      sources:
          - tests-kernel_defines.c
      depends:
          - riot_version
          - unittests-base
      env:
        global:
          testsuites:
              - kernel_defines
