app:
    depends:
        - tests-clif

module:
    - name: tests-clif
      sources: tests-clif.c
      depends:
          - clif
          - unittests-base
      global_vars:
          testsuites:
              - clif
