apps:
  - depends:
        - tests-inet_csum

modules:
    - name: tests-inet_csum
      sources: [tests-inet_csum.c]
      depends:
          - inet_csum
          - unittests-base
      env:
        global:
          testsuites:
              - inet_csum
