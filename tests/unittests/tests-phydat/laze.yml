app:
    - depends:
        - tests-phydat

module:
    - name: tests-phydat
      sources: [tests-phydat.c]
      depends:
          - phydat
          - unittests-base
      env:
        global:
          testsuites:
              - phydat
