apps:
  - depends:
        - tests-gnrc_ipv6
    blocklist:
        # insufficient memory
        - atmega_common

modules:
    - name: tests-gnrc_ipv6
      sources: [tests-gnrc_ipv6.c]
      depends:
          - gnrc_ipv6
          - unittests-base
      env:
        global:
          testsuites:
              - gnrc_ipv6
