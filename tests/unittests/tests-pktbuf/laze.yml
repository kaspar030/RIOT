apps:
    - depends:
        - tests-pktbuf

      blocklist:
          # insufficient memory
          - atmega_common

modules:
    - name: tests-pktbuf
      sources: [tests-pktbuf.c]
      depends:
          - gnrc_pktbuf_static
          - test_suites_define
          - unittests-base
      env:
        global:
          testsuites:
              - pktbuf
