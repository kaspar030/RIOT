app:
    depends:
        - tests-netopt

module:
    - name: tests-netopt
      sources: tests-netopt.c
      depends:
          - netopt
          - unittests-base
      global_vars:
          testsuites:
              - netopt
