apps:
    - depends:
        - tests-netopt

modules:
    - name: tests-netopt
      sources: [tests-netopt.c]
      depends:
          - netopt
          - unittests-base
      env:
        global:
          testsuites:
              - netopt
