apps:
  - depends:
        - tests-hashes
    blocklist:
        # insufficient memory
        - atmega_common

modules:
    - name: tests-hashes
      sources:
        - tests-hashes.c
        - tests-hashes-aes128_cmac.c
        - tests-hashes-md5.c
        - tests-hashes-sha1.c
        - tests-hashes-sha224.c
        - tests-hashes-sha256.c
        - tests-hashes-sha256-chain.c
        - tests-hashes-sha256-hmac.c
        - tests-hashes-sha3.c
      depends:
          - crypto_aes_256
          - hashes
          - unittests-base
      env:
        global:
          testsuites:
              - hashes
