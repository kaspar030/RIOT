apps:
    - depends:
        - tests-base64

modules:
    - name: tests-base64
      sources:
          - tests-base64.c
      depends:
          - base64url
          - unittests-base
      env:
        global:
          testsuites:
              - base64
