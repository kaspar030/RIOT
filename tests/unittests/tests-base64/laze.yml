app:
    depends:
        - tests-base64

module:
    - name: tests-base64
      sources: tests-base64.c
      depends:
          - sys/base64
          - unittests-base
      global_vars:
          testsuites:
              - base64
