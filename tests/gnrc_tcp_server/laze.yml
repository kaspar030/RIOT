app:
    depends:
        - auto_init_gnrc_netif
        - gnrc_ipv6_default
        - gnrc_netdev_default
        - gnrc_tcp
        - shell_commands
    export_vars:
        CFLAGS:
            - -DLOCAL_ADDR=\"fe80::affe\"
            - -DLOCAL_PORT=80
            - -DCYCLES=3
